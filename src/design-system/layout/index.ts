import { breakpoints } from '../tokens/breakpoints';
import { zindex } from '../tokens/zindex';

export const layout = {
  breakpoints,
  zindex,
} as const;

export { breakpoints, zindex };
export default layout;
